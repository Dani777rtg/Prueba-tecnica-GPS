{
  "name": "fleet-gps-telemetry",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Telemetría y Monitoreo de Flotas GPS",
  "scripts": {
    "db:up": "docker compose up -d",
    "db:migrate": "npm run db:migrate --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "simulator": "npm start --prefix simulator",
    "test": "npm test --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix simulator"
  }
}
